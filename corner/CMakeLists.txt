cmake_minimum_required(VERSION 2.8)
project( Corner )
set(CMAKE_BUILD_TYPE Debug)
find_package(OpenCV REQUIRED)
add_executable( DisplayCorners main.cpp)
target_link_libraries( DisplayCorners ${OpenCV_LIBS} )
